<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_pos_commande_inherited_two_form" model="ir.ui.view">
        <field name="name">pos.commande.form</field>
        <field name="model">pos.commande</field>
        <field name="inherit_id" ref="tit_pos_order.view_pos_commande_form"/>
        <field name="arch" type="xml">
           
            <xpath expr="//page[@name='ligne_cmd']" position="after">
                <page string="Informations supplémentaires" >
                    <label for="bon_livraison" string="Bon de livraison"/>
                    <field name="bon_livraison" />
                    <field name="bon_cmd" colspan="4" nolabel="1">
                        <tree string="Bons de commande">
                            <field name="name"/>
                            <field name="pos_reference"/>
                            <field name="partner_id"/>
                            <field name="amount_total"/>
                        </tree>
                    </field>
                </page>
            </xpath> 
        </field>
    </record>

</odoo>